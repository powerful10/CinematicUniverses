import React from "react";

export default function AntiheroesLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="antiheroes-layout">
      {children}
    </div>
  );
}
